<% if false %>
This is the page that shows the tip specified by the user. It displays the title 
of the tip, the name of the individual who posted it, the text, and the
associated comments.
<% end %>
  <p>
    <strong>Title:</strong>
    <%= @tip.title %>
  </p>
  
  <p>
    <strong>Name:</strong>
    <%= @tip.name %>
  </p>
   
  <p>
    <strong>Text:</strong>
    <%= @tip.text %>
  </p>
  
 <h2>Comments</h2>
  <%= render @tip.comments %>

  <h2>Add a comment:</h2>
  <%= render 'comments/form' %>
  
  <%= link_to 'Tips List', tips_path %>
