<%- #Specifies the view for the index page. This page lists all of the tips within the database, along with show, edit, and destroy buttons. -%>
<header class="welcomeheader">
  <h1>Listing Tips</h1>
</header>

<br>
 <%-# Table with titles included -%>
<table>
  <tr>
    <th>TITLE</th>
    <th>NAME</th>
    <th>TEXT</th>
  </tr>
 <%-# what each tip will do -%>
  <% @tips.each do |tip| %>
    <tr>
      <td><%= tip.title %></td>
      <td><%= tip.name %></td>
      <td><%= tip.text %></td>
      <td><%= link_to 'Show', tip_path(tip) %></td>
      <td><%= link_to 'Edit', edit_tip_path(tip) %></td>
 <%-#Destroy comment  -%>
      <td><%= link_to 'Destroy',
                                tip_path(tip),
                                method: :delete,
                                data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
<%-# Link to add a new tip that will direct you the appropriate page -%>
<%= link_to 'Add New Tip', new_tip_path %>